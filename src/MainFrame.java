import javax.swing.JFrame;

public class MainFrame extends JFrame {
    public MainFrame(String title) {
        super();
        setTitle(title);
        setVisible(true);
        setSize(400, 600);
    }
}
